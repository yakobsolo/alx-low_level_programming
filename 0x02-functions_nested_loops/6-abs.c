#include <stdlib.h>
#include "main.h"
/**
 * _abs - function that compute the absolute value of an integer
 * @c: accepts an integer c
 * Return: retruns res
 */
int _abs(int c)
{
	int res = abs(c);

	return (res);
}
