#include <stdio.h>
#include "main.h"
/**
 * mul - function to multiply to integer
 * @a: parameter a
 * @b: parameter b
 * Return: return 0
 */
int mul(int a, int b)
{
	int c = 0;

	c = a * b;
	printf("%d", c);
	return (0);
}
