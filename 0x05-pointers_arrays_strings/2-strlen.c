#include <string.h>
#include "main.h"
/**
 * _strlen - function to calculate the string length
 * @s: parameter s
 * Return: returns integer
 */
int _strlen(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
